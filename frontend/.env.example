# 로컬 개발용 설정 예시 파일입니다.
# 이 파일을 `.env.local`로 복사한 뒤 필요한 값을 채워 넣으세요.
# (.env.local은 frontend/.gitignore에 의해 Git에 커밋되지 않습니다.)
#
# 값을 지정하지 않으면 기존과 동일하게 http://localhost:8080을 사용합니다.
VITE_API_BASE_URL=http://localhost:8080
